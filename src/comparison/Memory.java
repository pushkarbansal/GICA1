package comparison;

public class Memory {
    float memoryelapsed;
        public Memory(float memoryelapsed) {
            this.memoryelapsed = memoryelapsed;
            System.out.println(memoryelapsed);
        }
    
}
